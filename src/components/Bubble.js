import PropTypes from 'prop-types';
import React from 'react';

const Bubble = (props) => {
  return (
    <div className='bubble-Comp'>

    </div>
  );
};

export default Bubble;

Bubble.propTypes = {
  classes: PropTypes.array.isRequired
};

